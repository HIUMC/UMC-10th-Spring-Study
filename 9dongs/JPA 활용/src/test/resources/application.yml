spring:
#  스프링 부트는 기본적인 설정 없으면 메모리 모드로 돌려줌
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate :
#        # show_sql: true # 시스템 아웃으로 찍힘
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug # 로거로 찍힘
    org.hibernate.orm.jdbc.bind : trace