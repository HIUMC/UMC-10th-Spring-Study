# 테스트 케이스용 설정을 따로 할 수 있다.

# 스프링 부트는 별도의 설정이 없으면 기본적으로 메모리 모드로 돌린다. 설정 없어도 동작!

spring:
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 DB
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace #스프링 부트 3.x, hibernate6 -> 쿼리 파라미터를 확인할 수 있다.